/* LeetCode Problem #0: 103. Binary Tree Zigzag Level Order Traversal
 * Difficulty: Medium
 * Topics: Tree, Breadth-First Search, Binary Tree
 * Language: Unknown
 * Runtime: N/A | Memory: N/A
 * Solved: 9/12/2026
 */

                q.remove();
                res.add(node.val);
              
                if (node.left!=null) q.add(node.left);
                if (node.right!=null) q.add(node.right);
                if (level % 2 == 0) Collections.reverse(res);
            }
            
            ans.add(res);
            level++;
        }
        return ans;
    }
    
}
